{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dashboard-window",
  "description": "대시보드 창에 주는 최소 권한. 이 창은 앱이 정의한 dashboard_* 명령만 쓴다",
  "windows": [
    "dashboard"
  ],
  "permissions": [
    "core:default"
  ]
}
